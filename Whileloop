#include <stdio.h>

int main() {
    int num = 1;

    // Print numbers from 1 to 10 using a while loop
    while (num <= 10) {
        printf("%d\n", num);
        num++; // Increment the counter
    }

    return 0;
}
